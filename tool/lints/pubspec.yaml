name: mentormind_lints
description: Project-local custom_lint rules for MentorMinds (layered_imports).
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  custom_lint_builder: ^0.7.0
  analyzer: ^7.0.0
